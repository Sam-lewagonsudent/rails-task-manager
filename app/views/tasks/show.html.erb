<h2><%= @task.title %></h2><%= link_to "edit", edit_path(@task)%>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to "back to list", tasks_path(@task)%>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
